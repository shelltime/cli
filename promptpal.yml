input:
    http:
        url: 'https://pp.shelltime.xyz'
        token: '@env.PROMPTPAL_API_TOKEN'
output:
    schema: ./schema.g.json
    go_types:
        prefix: PP
        package_name: model
        output: ./model/pp.types.g.go